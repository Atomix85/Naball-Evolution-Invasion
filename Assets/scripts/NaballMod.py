#List of imported modules
import *

print("Naball Moder 1.00")
try:
	naballMod = SyntaxeMod()
	naballMod.Start()
	naballMod.Update()
except:
	print("Failed to start : Naball Starter 1.00")
	
class SyntaxeMod:
	
	def __init__():
		self.alStarter = True
	def Start(self):
		if self.autorun:
			self.alStarter = False
			#List of commands to execute at startup
			
		
	def Update(self):
		#List of commands to execute at every tics
		
		